{
  "source": "src/index.js",
  "module": "dist/isdt-charger-card.js",
  "version": "3.0.0",
  "targets": {
    "module": {
      "includeNodeModules": true
    }
  },
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build src/index.js --dist-dir dist --no-source-maps"
  },
  "devDependencies": {
    "parcel": "^2.16.1"
  }
}
